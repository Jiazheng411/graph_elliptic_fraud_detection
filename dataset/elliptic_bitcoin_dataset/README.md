# Elliptic Bitcoin Transactions Sample Set

Contains samples from the original dataset.

## Files
- `elliptic_txs_features_labelled_only` contains the tabular features with label as the last column
- `elliptic_txs_features_labelled_only_with_graph_feature` contains the tabular + graph features (currently only in-degree) with label as the last column